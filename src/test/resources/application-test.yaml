spring.cache.type: none
spring:
  init:
    mode: always
  datasource:
    url: jdbc:h2:mem:jira-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;NON_KEYWORDS=VALUE
    username: jira
    password: JiraRush
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console